#ifndef JULI_H
#define JULI_H
int sign_up(void);
int log_in(void) ;/*this is the function by which will allow a user to log in*/
int rec_addition(void); /*this is the function by which the user wwill use to add new information about the new mortgage*/
int display(void) ;/*this function will bne the one that calls up what the user sees depending on the mortgage he chose to see*/
float calculate(int a,int b,int c, int d);
#endif